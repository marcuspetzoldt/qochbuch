<tr>
  <td><%= link_to units.name, edit_unit_path(units.id) %></td>
  <td><%= units.other %></td>
  <td><%= units.taggings.count %></td>
  <td>
    <%= link_to unit_path(units.id), method: 'delete', title: t('views.units.delete') do %>
      <i class="icon-remove"></i>
    <% end %>
  </td>
</tr>
